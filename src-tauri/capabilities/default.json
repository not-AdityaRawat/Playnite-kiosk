{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capabilities for the primary Playnite window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
